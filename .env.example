# TheWrker — environment variables
# Copy to .env.local for local development.

# Public site URL (used for social preview links when sharing)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ── Optional API keys (server-side only) ─────────────────────────────────────

GOOGLE_CSE_API_KEY=
GOOGLE_CSE_CX=
SERPAPI_API_KEY=
OCR_SPACE_API_KEY=
REMOVE_BG_API_KEY=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
